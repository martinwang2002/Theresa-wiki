import { styled } from "@mui/system"

import { TileBase } from "./tileBase"

export const TileUndefined = styled(TileBase)({
  backgroundImage: "url(\"https://static.theresa.wiki/api/v0/AK/CN/Android/missingTile\")",
  backgroundSize: "contain"
})
